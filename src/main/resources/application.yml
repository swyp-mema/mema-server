server:
  port: 8080

spring:
  profiles:
    active: dev

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html

api:
  info:
    key: ${API_KEY}
  time:
    key: ${API_TIME_KEY}
  master:
    key: ${API_MASTER_KEY}